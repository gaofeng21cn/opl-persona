[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "opl-persona"
version = "0.1.2"
description = "Evidence-backed cross-domain personal context and output proposals for OPL."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["plugins/opl-persona/runtime"]
testpaths = ["tests"]
